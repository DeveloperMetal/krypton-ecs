{
  "name": "framework",
  "version": "1.0.0",
  "scripts": {
    "prepare": "yarn build",
    "build": "tsc",
    "test": "jest --coverage",
    "watch:test": "jest --watchAll --coverage"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "@types/jest": "^26.0.14",
    "cross-env": "^7.0.2",
    "jest": "^26.5.3",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "testRegex": "^.+\\.spec\\.ts$"
  }
}
